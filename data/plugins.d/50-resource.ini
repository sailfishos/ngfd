# media.TYPE = BOOLEAN
# TYPE part is used for the sink interface type string,
# if BOOLEAN is true then the sink is allowed through
# by default (ie not filtered out), if false then
# the flag needs to be explicitly set as true for
# a request to let the sink types through filtering.

[resource]
media.audio  = true
media.vibra  = true
media.leds   = true
